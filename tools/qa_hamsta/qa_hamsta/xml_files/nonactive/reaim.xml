<?xml version="1.0"?>
<job>
    <config>
        <name>reaim</name>
        <debuglevel>4</debuglevel>
        <distributable>0</distributable>
        <mail notify="0">vmarsik@suse.cz</mail>        
	<description>runs the reaim benchmark</description>
	<logdir>/tmp</logdir>
	<rpm>hamsta reaim-ctcs2-glue</rpm>
    </config>

    <commands>
    <worker>
	    <!-- Pre -->
	    <command execution="forked">#!/bin/bash
		echo Running Reaim in Screen
		screen -S reaim-hamsta -L -d -m /bin/bash -c '/usr/lib/ctcs2/tools/reaim-run'
		sleep 1
		while screen -ls | grep reaim-hamsta > /dev/null ; do sleep 1; done
		echo Done
           </command>

	   <directory>/var/log/qa/ctcs2</directory>
     </worker>
    </commands>
</job>
